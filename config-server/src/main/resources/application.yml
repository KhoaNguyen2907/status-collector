server:
  port: 8888
logging:
  level:
    ROOT: INFO
    org.spring.framework.cloud.config: DEBUG
spring:
  application:
    name: configserver
  cloud:
    config:
      server:
        git:
          uri: file:///Users/khoanguyen/GitHub/status-collector/config-server-repository/
          clone-on-start: true
          default-label: master
    fail-fast: true
